# IPAM-Lite
A lightweight IP inventory tool for small operations teams. Clean UI, single-purpose functionality. No database service to install, no web server to configure—just one Python file and one SQLite file and it runs directly. View, add, edit, filter, and export IPs in the browser; data travels with the files.
